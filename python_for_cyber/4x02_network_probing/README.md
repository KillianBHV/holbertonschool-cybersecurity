# PYTHON - Network Probing

---

### 0. The Environment
Goal: Set up the project environment

Files Solution: **`net_probe.py`**, **`requirements.txt`**, **`venv/`**,

Expected Output:
```bash
$ python3 -m venv venv
$ ls -a
.  ..  .gitignore  net_probe.py  README.md  requirements.txt  venv/
$ source venv/bin/activate
(env)$ ./net_probe.py
NetProbe v1.0 initialized...
```

### 0. X
Goal: Y
File Solution: Z
Expected Output: **`None`**
