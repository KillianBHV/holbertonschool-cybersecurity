# Source Material Critical Review

|Finding|First impression|Zone or doubt or confidence|To investigate in Task 3|
|---|---|---|---|
|F-01|She discovered that two laboratories can pretend being the other one for retrieving results, because of uncontrolled identifiers substitutions.|The given proof is clear and seems effectively reproductible and gives laboratories flaw evidence. There's no immediate reason to change the case severity.|Due to implication of health data, review of data sensibility returned fields with conformance and potential likelihood of this finding. 
|F-02|Sarah emphazises that accepted marked are executed by accessing the target case, because of the portal for accessing case does not complete the output encoding full process|Given proof is explicit enough for active-content sanitizing itself. But it does not confirm, ensure that pre-process data, at network-level, are effectively sanitized, that complete flaw relies on application-level sanitization.|Review the effective areas of laboratory access and CSP current policy.
|F-03|She discovered a large-scope perimeter of connector service token with a long-live time validity (180 days; 1/2 year).|Explicit site-to-site connector invocation mentioned but much clarity about given internal routes from gateway and controls.|Just ensure token is actually time-based and much larger intented scope. 
|F-04|Sarah discovered that upstream certificates are not properly checked, because untrusted ones are permit by the worker.|Require much clarification about the effective certification validator machine and verified data flow|Review in priority CVSS Attack Vector and Availability Level and ensure implied traffic effectively pass through internal managed gateway.
|F-05|Sarah discovered that for the webhook, it is possible to alter status because of a lack of independent body authentication|Evidence of a required separate process currently combined and modification of requests.|Ensuring of valid states and non-application, checks presence of cryptographic signature, acting as a proof of status validation.|
|F-06|Sarah discovered a discontinuity between effective exports packets (24 hours) and the current 30 days valid window of their signed links, acting as a validation process based on time.|Deletion and synchronisation are twice mentioned explicitly.|Only checks conformance of data in the time-based perspective. 
|F-07|Patient export comes from multiple laboratories and returns high-volume data containing all-in-one information.|Explicitly defines the role can act as an other, but not about returned data themselves.|Needs a more explicit role authorization and laboratories scopes for initiation and checks data are only required ones, not ones which can overload the bulk.
|F-08|Sarah discovered that the password-recovery process distinguishes existing from non-existing accounts and saw that password checking relies on source address rather than unique IDs.|Password-itself are grea, but monitoring once are not really explicit, only mentioned.|Review password-recovery mechanisms by ensuring each control don't contradict another one.
|F-09|She discovered that after a downgrading to read-only role, an administrator is able to perform high-level actions anymore, because of authorization caches expiration current parameter.|Finding clearly defined, state is credible and is checkable. |Review time-based given roles by groups, and the updating window status consideration. 
|F-10|Sarah discovered that a custom header inserted by API allows to bypass the gateway.|There is no established problem scope disgression. Just ensure what services pass through the gateway.|Just check the coherent of correct path and right retrieving according to F-01.
